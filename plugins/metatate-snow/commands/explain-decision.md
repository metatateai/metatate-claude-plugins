---
description: Explain a prior Metatate authorization decision or query validation.
---

# Explain Metatate Decision

Use the `metatate` MCP tool `explain-why`.

Pass `decision_id` for authorization decisions or `validation_id` for query
validations. Summarize the outcome, decisive factors, policy/rule evidence,
trace type, and next action.
